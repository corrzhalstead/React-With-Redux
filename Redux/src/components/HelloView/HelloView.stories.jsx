import React from "react";

import { HelloView } from "./HelloView";

export default {
  title: "Bonus Components/HelloView",
  component: HelloView,
};

const Template = () => <HelloView />;
export const Default = Template.bind({});
