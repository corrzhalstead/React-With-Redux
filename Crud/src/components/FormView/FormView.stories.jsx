import React from "react";
import { FormView } from "./FormView";

export default {
  title: "Components/FormView",
  component: FormView,
};

const Template = () => <FormView />;

// export const Logo = Template.bind({});
// Logo.args = {};

export const SharedForm = Template.bind({});
